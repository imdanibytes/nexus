#!/usr/bin/env bash
set -e

echo "pre-push: running clippy..."
(cd src-tauri && cargo clippy -- -D warnings)

echo "pre-push: running tsc..."
pnpm tsc -b

echo "pre-push: running eslint..."
pnpm lint

echo "pre-push: all checks passed"
